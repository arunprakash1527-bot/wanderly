import React from 'react'
import ReactDOM from 'react-dom/client'
import WanderlyApp from '../wanderly-app.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WanderlyApp />
  </React.StrictMode>,
)
